name: Release Package (Manual)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Release handled by CI workflow
        run: echo "NuGet publish now runs from .github/workflows/ci.yml on push to main."
